# ONLINE_BANKING-SYSTEM, 2025556283. 
The online banking system is excutable in c/c++ or gcc compile.
The online banking system consist of files, that takes input imformation of each of the user.
The code was built and runned using Visual Studio code.

  ASSUMPIONS
Limited accounts: The system assumes a maximum of 50 accounts beyound that, registration fails.
No concurrency: Assumes only one user interacts at a time no multithreading or simultaneous access.

   LIMITATIONS
Security limitations: paasword are stored in a plain text (no encrytion or hashing). input is visible on screen(no hidden password entry).
User interface: The code is console based, GPU was not used in this code.

course code Dip ICT 13,
